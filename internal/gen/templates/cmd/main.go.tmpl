
// Package main is the entry point for the {{.Server.Name}} MCP server.
package main

import (
	"log"
)

func main() {
	log.Println("{{.Server.Name}} — scaffolded by mcp-go-gen; handler wiring lands in a subsequent phase")
}
